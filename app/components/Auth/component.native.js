import React from 'react';
import App from '../App';

const Auth = (props) => {
  // const history = syncHistoryWithStore(browserHistory, store)
  return (
      <App {...props} />
  )
}

export default Auth;
